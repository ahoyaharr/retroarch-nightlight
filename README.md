# retroarch-nightlight
Add `nightlight.glslp` to the directory with your other Retroarch presets, and add `shaders/color/nightlight.glsl", preserving the folder structure. Enable it using the preset (`nightlight.glslp`), not by trying to add the shaderfile itself. 

